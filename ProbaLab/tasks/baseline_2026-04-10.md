# Baseline couverture tests — 2026-04-10

Relevé au démarrage de l'exécution du plan `01_PROBALAB_actions_correctives.md`, branche `chore/audit-2026-04-10`.

## Résumé global

- **265 tests passent** (suite unitaire, hors integration)
- **178 tests marqués `@pytest.mark.integration` et déselectionnés**
- **Couverture globale : 19%** (12 331 lignes non couvertes sur 15 304)
- **3 fichiers de test cassés** (à fixer/marquer integration en Phase 1) :
  - `tests/test_db.py`
  - `tests/test_db_cs.py`
  - `tests/test_dembele.py`
  - Tous tentent une connexion Supabase à l'import → `httpx.ConnectError`

Discordance notable avec l'audit : l'audit affiche « 381 tests / couverture 40% », la réalité locale donne 265 tests unitaires qui passent et 19% de couverture globale. Le seuil CI `--cov-fail-under=40` devrait donc faire échouer la CI sur `main` — à vérifier.

## Couverture des fichiers critiques (cibles du plan)

| Fichier | Lignes | Couverture | Statut |
|---|---|---|---|
| `api/routers/best_bets.py` | 838 | **16%** | CRITIQUE — résolution WON/LOST, ROI, Kelly |
| `api/routers/trigger.py` | 849 | **10%** | CRITIQUE — orchestration pipeline ML |
| `src/brain.py` | 167 | 12% | Core prédiction |
| `src/prediction_blender.py` | 121 | 55% | Blending 70/30 |
| `src/bankroll.py` | 118 | 77% | Kelly + bankroll (déjà bien testé) |
| `src/monitoring/brier_monitor.py` | 180 | **0%** | |
| `src/monitoring/alerting.py` | 72 | **0%** | |
| `src/monitoring/drift_detector.py` | 63 | **0%** | |
| `src/monitoring/feature_audit.py` | 65 | **0%** | |
| `src/monitoring/data_quality.py` | 120 | **0%** | |
| `src/monitoring/backtest_clv.py` | 123 | **0%** | |
| `api/schemas.py` | 44 | 89% | |
| `api/auth.py` | 35 | 57% | |
| `api/rate_limit.py` | 24 | 58% | |
| `src/models/schemas.py` | 100 | **0%** | |
| `src/ticket_generator.py` | 507 | **0%** | |
| `src/training/train.py` | 343 | **0%** | |

## Cibles après Phase 1

- Couverture globale : **19% → 60%**
- `best_bets.py` : **16% → 80%**
- `trigger.py` : **10% → 60%**
- `prediction_blender.py` : **55% → 85%**
- `bankroll.py` : **77% → 90%**

## Cibles après Phase 2

- `src/monitoring/*` : **0% → 70%** (au moins sur les fonctions publiques)

## Commande de reproduction

```bash
cd ProbaLab
set -a && . src/.env && set +a
pyenv exec pytest tests/ -m "not integration" \
  --timeout=60 --cov=src --cov=api --cov-report=term \
  --ignore=tests/test_db.py \
  --ignore=tests/test_db_cs.py \
  --ignore=tests/test_dembele.py -q
```

## Notes

- Le premier run sans env var chargé donnait 263 passed + 2 failed (test_stats_engine flaky sur Supabase client init). Flakiness env-dependent à résoudre en Phase 1.
- Le `conftest.py` fournit `MockSupabaseQuery` mais n'est pas appliqué automatiquement — plusieurs tests tapent la vraie DB via `from src.config import supabase`.
