# Lessons Learned

| Date | Problème | Règle |
|------|----------|-------|
| 2026-03-19 | Rho scaling discontinu : interpolation linéaire avec coefficient 0.4 au lieu de 0.6, saut de 22% à xG=3.5 | Toujours vérifier la continuité aux bornes des interpolations linéaires par morceaux |
| 2026-03-19 | poisson_grid retournait sum≠100 (99 ou 101) — arrondis indépendants de 3 probas | Pour N probas sommant à 100%, toujours forcer le dernier = 100 - somme(autres) |
| 2026-03-19 | MAX_GOALS_GRID=7 : 11% de masse Poisson perdue à xG=4.0. Grid=10 ramène à 0.8% | Dimensionner les grilles pour capturer ≥99% de la masse aux valeurs extrêmes autorisées |
| 2026-03-19 | Double comptage nuls CL : draw calibration cible déjà le taux historique, euro_boost ajoutait +4% en plus | Quand un paramètre est déjà calibré sur des données historiques, ne pas ajouter de boost ad hoc sur le même signal |
| 2026-03-19 | Ensemble sans class balancing : les 3 base learners ignoraient le déséquilibre H/D/A | Vérifier que TOUS les modèles d'un pipeline reçoivent sample_weight, pas seulement le standalone |
| 2026-03-19 | eval_set=(X_test, y_test) dans ensemble : le modèle voit le test set pendant l'entraînement | Toujours utiliser un validation set séparé pour l'early stopping, jamais le test set |
| 2026-03-19 | RestrictedUnpickler startswith("sklearn") trop large : n'importe quel sous-module sklearn autorisé | Whitelist par préfixes spécifiques, pas par un startswith global |
| 2026-03-22 | Data leakage : imputer.fit_transform(X) sur tout le dataset avant le TimeSeriesSplit — les médianes du futur fuitent dans le train | Toujours fit l'imputer sur X_train APRÈS le split, puis transform X_test séparément |
| 2026-03-22 | cross_val_score(params={"sample_weight": ...}) silencieusement ignoré — le param correct est fit_params= | Vérifier la doc sklearn pour les noms de paramètres exacts, surtout les kwargs relayés |
| 2026-03-22 | Fallback proba_map {"H": probas[0]} faux : LabelEncoder trie alphabétiquement → probas[0]=A, pas H | Sans LabelEncoder, toujours utiliser l'ordre alphabétique ['A','D','H'] pour mapper les probas sklearn |
| 2026-03-22 | Noms d'équipes injectés directement dans les prompts Gemini via f-strings — injection de prompt possible | Toujours sanitiser les inputs utilisateur/DB avant injection dans un prompt LLM |
| 2026-03-22 | Appel Gemini sans retry : un timeout ou erreur transitoire fait perdre toute l'analyse IA du match | Toujours wrapper les appels API externes avec au moins 1 retry + sleep |
| 2026-03-22 | Variables injectées dans du HTML Telegram sans échappement — risque XSS/formatting cassé | Toujours html.escape() les variables avant injection dans du HTML |
| 2026-03-22 | pickle.load non sécurisé dans nhl.py router — modèle chargé sans restriction | Toujours utiliser un RestrictedUnpickler pour charger des fichiers pickle, même pour des modèles internes |
| 2026-03-22 | HTTPException detail=str(e) expose des infos internes (stack, DB schema) | Toujours logger l'erreur côté serveur et retourner un message générique au client |
| 2026-03-22 | Comparaison CRON_SECRET via == vulnérable au timing attack | Utiliser hmac.compare_digest pour toute comparaison de secrets |
| 2026-03-22 | api_get retournait HTTP 200 avec response=[] sans retry — données perdues silencieusement | Ajouter un second niveau de retry (api_get_with_retry) pour les endpoints critiques qui retournent des réponses vides |
| 2026-03-22 | datetime.now() sans timezone dans config.py et matches.py — comparaisons naive vs aware impossibles | Toujours utiliser datetime.now(timezone.utc) — jamais datetime.now() nu |
| 2026-03-22 | place_bet read-then-write non atomique : deux paris simultanés lisent le même bankroll | Pour toute opération read-modify-write sur une ressource partagée, utiliser SELECT FOR UPDATE dans une fonction PostgreSQL (RPC) |
| 2026-03-22 | BTTS accuracy utilisait total_with_pred comme dénominateur au lieu d'un compteur dédié — les matchs sans proba_btts comptaient comme "No BTTS" | Chaque marché doit avoir son propre compteur total, comme Over/Under le faisait déjà |
| 2026-03-30 | proba_over_05/15/35 sauvés dans stats_json mais pas en colonnes DB — best-bets les lisait comme NULL, Over 1.5/3.5 jamais générés | Quand une colonne DB existe pour un champ, toujours l'inclure dans insert_data, pas seulement dans stats_json |
| 2026-03-30 | `get_val("x") or get_val("y")` — si x vaut 0 (falsy), fallback inattendu sur y | Toujours utiliser `if x is None` au lieu de `or` pour des valeurs numériques qui peuvent être 0 |
| 2026-03-30 | Combo resolve : `all_win = None` écrasait `all_win = False` — combos LOSS restaient PENDING | Séparer les flags booléens : un pour le résultat (all_win), un autre pour les données manquantes (has_unknown) |
| 2026-04-02 | Railway déployait le code racine au lieu de ProbaLab/ — tous les fixes récents étaient invisibles en prod | Quand un projet est réorganisé dans un sous-dossier, toujours supprimer les fichiers racine de git ET configurer Railway Root Directory |
| 2026-04-02 | Fichiers dupliqués racine/ProbaLab désynchronisés — 11 fichiers divergeaient silencieusement | Une seule source de vérité : jamais de doublons dans le repo, supprimer les copies obsolètes immédiatement |
| 2026-04-03 | `predicted_result` utilisait `>=` → Home gagnait tous les tie-breaks de probas, biaisant l'accuracy 1X2 vers le haut | Toujours utiliser `>` strict pour le tie-breaking de probas ; les égalités ne doivent pas être comptées dans l'accuracy |
| 2026-04-03 | `get_val("proba_home", 33)` — les prédictions sans probas recevaient 33/33/33 par défaut et étaient comptées dans l'accuracy | Ne jamais utiliser de valeur par défaut pour les probas ; skip la prédiction si les probas sont NULL |
| 2026-04-03 | `pred_by_fixture` dict gardait la dernière prédiction (ordre Supabase non déterministe) — si re-runs plus précises, accuracy gonflée | Toujours dédupliquer en gardant la PREMIÈRE prédiction (ORDER BY created_at ASC) |
| 2026-04-03 | Over 0.5/1.5/3.5 n'avaient de probas que pour 14-20% des prédictions mais l'accuracy était affichée sans contexte | Toujours afficher les métriques de couverture (total_xxx) avec les taux d'accuracy pour éviter les conclusions trompeuses |
| 2026-04-03 | DELETE expert-picks n'avait aucune auth (commentaire "enforced in UI") — suppression publique possible | Ne jamais compter sur le frontend pour l'auth ; tout endpoint sensible doit avoir une vérification serveur |
| 2026-04-03 | Meta-learner (WEIGHT_AI > 0) activé en test car les fichiers .ubj existent sur disk — tests brain.py ont échoué | Toujours ajouter un feature flag dédié (META_LEARNER_ENABLED) pour les composants expérimentaux, ne pas dépendre d'un poids non-nul comme seul gate |
| 2026-04-03 | Stripe webhook check-then-act (SELECT puis INSERT) = race condition entre workers | Toujours utiliser INSERT atomique + gestion erreur unique constraint, jamais SELECT-then-INSERT |
| 2026-04-03 | Telegram webhook ouvert si TELEGRAM_WEBHOOK_SECRET absent (fail-open) | Les webhooks doivent être fail-closed : si le secret est absent, rejeter toutes les requêtes (503) |
| 2026-04-03 | Combo avec leg VOID restait PENDING éternellement — `all_win = False` mais pas `has_loss` → aucun état final | Dans les combos, traiter VOID comme absent : filtrer non_void_legs, évaluer WIN/LOSS/VOID sur cette sous-liste uniquement |
| 2026-04-03 | Fuzzy match substring uniquement — ne matchait pas si l'ordre ou la casse différait | Utiliser difflib.SequenceMatcher en fallback après le match substring exact ; seuil 0.4 évite les faux positifs |
| 2026-04-03 | train.py upsert(on_conflict="model_name") écrasait le bon modèle actif même quand le nouveau était pire | Quand un nouveau modèle a un Brier >= l'ancien, ne PAS upsert du tout — skip le save pour préserver le modèle actif |
| 2026-04-03 | Rate limiting silently disabled si slowapi absent (ImportError catch → no-op) | Les dépendances de sécurité doivent être fail-loud : logger CRITICAL au démarrage si absentes |
| 2026-04-03 | Aucun security header (X-Frame-Options, HSTS, etc.) sur les réponses API | Ajouter un SecurityHeadersMiddleware systématique sur toute app FastAPI en production |
| 2026-04-03 | Pas d'audit log admin — impossible de tracer qui a fait quoi | Logger source (cron/jwt) + user_id dans _verify_internal_auth() pour chaque action admin |
| 2026-04-04 | 26 sys.path.insert() hacks dans le codebase — les imports ne fonctionnaient qu'en exécutant depuis le bon répertoire | Utiliser pyproject.toml + pip install -e . pour un vrai package Python ; les sys.path.insert() sont un anti-pattern à bannir |
| 2026-04-04 | pyproject.toml requires-python=">=3.11" mais pyenv local est 3.10.12 — pip refusait d'installer | Toujours vérifier la version Python locale (pyenv exec python --version) avant de fixer requires-python |
| 2026-04-04 | `pip install` dans un projet avec pyenv utilisait le mauvais interpréteur système — module non trouvé au runtime | Toujours utiliser `pyenv exec pip install` pour installer dans l'environnement pyenv du projet |
| 2026-04-04 | `from src.fetchers.matches import LEAGUES_TO_FETCH` dans performance.py declenchait exit() + second client Supabase via les side-effects du module matches.py | Ne jamais importer une constante depuis un module avec des side-effects (exit, create_client, etc.) — deplacer les constantes dans un module pur (constants.py) |
| 2026-04-04 | fixtures.id est TEXT (UUIDs + IDs numeriques) mais predictions.fixture_id est INTEGER — .in_() avec UUIDs cause PostgreSQL 22P02 | Toujours filtrer les IDs par type avant un .in_() cross-table quand les types de colonnes different (str vs int) |
| 2026-04-05 | Bayesian Shrinkage 1X2 tirait les probas vers 45/27/28 même avec 400+ échantillons — Brier Score quasi-aléatoire (0.317 vs 0.33) | Conditionner les calibrations conservatrices (shrinkage) sur le nombre d'échantillons ; skip quand n >= 200 pour laisser le signal passer |
| 2026-04-18 | sample_weight passé via params= silencieusement ignoré dans cross_val_score → CV ne corrigeait pas l'imbalance H/D/A | Pour cross_val_score, utiliser fit_params= (PAS params=). Test AST de régression ajouté |
| 2026-04-05 | XGBoost softmax probabilities non calibrées en sortie — le modèle est overconfident sur certaines classes, dégradant le Brier Score | Toujours ajouter une couche de calibration post-training (IsotonicRegression per-class) sur un jeu de calibration holdout |
| 2026-04-05 | Page Performance NFL n'affichait pas le Brier Score alors que le calcul est trivial (binaire) et les données déjà disponibles | Ne jamais gater une métrique de qualité par sport quand la formule est générique — appliquer à tous les sports |
| 2026-04-05 | Le signal bookmaker (52-55% accuracy) ne pesait que 21% du résultat final — dilué par Poisson (27%) et ELO (12%) qui sont moins précis | Le signal le plus informatif doit avoir le poids dominant ; les signaux faibles servent de sanity check, pas de co-prédicteurs égaux |
| 2026-04-05 | round() à chaque étape du pipeline (5 stages × 3 variables) cumulait ~2-3% d'erreur d'arrondi → perte de gradients de probabilité | Garder la précision float tout au long du pipeline, arrondir une seule fois à la sortie finale |
| 2026-04-05 | text-[9px] et text-[10px] utilisés sur des infos critiques (edge, odds, probas) — illisibles sur mobile | Minimum text-xs (12px) pour tout label, text-sm (14px) pour tout contenu interactif ; bannir les tailles custom < 12px |
| 2026-04-05 | La feature core (pronos quotidiens) était enterrée au 4ème tab et cachée pour les users free — 3+ clics pour y arriver | La feature principale doit être à 1 clic max de n'importe quelle page ; visible pour TOUS les users (avec gate progressive, pas blocage total) |
| 2026-04-05 | Dashboard football avait 2 cards expandables (Meta-Analysis + Top Markets) qui faisaient du bruit visuel avant les matchs | Un dashboard de consultation doit être clean : date + filtres + contenu. Les analyses détaillées appartiennent à la page de détail |
| 2026-04-05 | `edge_pct = round(ev * 100, 1)` dans best_bets.py — EV (prob*odds-1) confondu avec edge (prob - 1/odds) → valeurs x4-5 trop élevées (+90% affiché au lieu de +19%) | Edge et EV sont deux métriques distinctes : edge = model_prob - implied_prob ; EV = model_prob * odds - 1. Ne jamais les confondre ni dériver l'un de l'autre |
| 2026-04-10 | Backend utilisait la clé anon pour TOUS les accès Supabase — aucune table n'avait RLS activé en production | Le backend doit toujours utiliser service_role (bypasse RLS) ; la clé anon est réservée au frontend avec des policies strictes |
| 2026-04-10 | `combined_last_10` retournait des strings ["WIN","LOSS"] mais le frontend attendait des objets {result, label, odds} → @0.00 affiché | Quand une API alimente un composant UI, toujours vérifier que la shape des données correspond exactement à ce que le frontend consomme |
| 2026-04-10 | `--cov-fail-under=40` alors que la couverture réelle était à 19% → CI permanently rouge, personne ne regardait plus les échecs | Un seuil de couverture doit être ≤ à la couverture actuelle mesurée. Mesurer d'abord, fixer ensuite, remonter par paliers au fur et à mesure des ajouts de tests |
| 2026-04-10 | Scripts de debug (`test_*.py`) à la racine ET dans `tests/` qui connectaient Supabase à l'import → 3 fichiers cassaient la collecte pytest + faux baseline | Ne jamais laisser de scripts jetables dans `tests/` — soit les mettre dans un dossier séparé (`scripts/debug/`), soit les supprimer dès qu'ils ne servent plus |
| 2026-04-10 | Fonctions métier imbriquées dans des endpoints FastAPI (`_evaluate_single_football_market` dans `resolve_best_bets`) → impossibles à tester sans TestClient + mocks lourds | Extraire la logique pure dans un module séparé (`best_bets_logic.py`) dès qu'une fonction a une valeur de test unitaire — routers FastAPI = glue, logique = module pur |
| 2026-04-10 | `@_rate_limit("10/minute")` de slowapi exige une vraie `starlette.Request`, impossible à construire avec un simple `MagicMock` | Pour tester un endpoint décoré par slowapi sans TestClient, utiliser `resolve_best_bets.__wrapped__(...)` qui expose la fonction originale non décorée |
| 2026-04-10 | Migration Trigger.dev → APScheduler incomplète : `scheduleNHLPipeline` (Trigger.dev) désactivé mais jamais porté dans `worker.py`, donc aucun match NHL généré depuis la consolidation — `job_nhl_picks` à 17h lisait un `nhl_fixtures` vide | Quand on désactive un scheduler externe au profit d'un scheduler interne, faire un diff exhaustif des tâches **avant** de gate sur une env var — inventaire ligne à ligne, pas à la mémoire |
| 2026-04-10 | `fetch_schedule()` NHL : si today est dans `gameWeek` avec `games=[]` (jour off, fin saison régulière), return immédiat `([], today)` — le fallback "closest day with games" ne se déclenchait jamais, ratant les matchs du lendemain | Un early-return dans une boucle ne doit pas matcher la clé mais bien la valeur utile : `if day["date"] == today and day.get("games"):` plutôt que `if day["date"] == today:` — sinon le fallback prévu pour ce cas devient inatteignable |
| 2026-04-17 | F823 dans `api/evaluate_predictions.py` et `src/fetchers/nhl_pipeline.py` : réimport local de `timezone` après usage à un scope supérieur → UnboundLocalError possible si le code top-level était exécuté avant. Et F821 `prob_assist` dans `api/routers/nhl.py` : la calibration ASSIST manquait, exposant `raw_prob_assist` directement dans la response → variable undefined levait NameError à l'exécution | Les ré-imports `from datetime import timezone` dans une fonction qui utilise déjà `timezone` du module créent un shadowing local. Soit on importe uniquement au top-level, soit on importe proprement dans la fonction dès le début. Et vérifier que chaque variable utilisée dans un response model est bien définie dans toutes les branches (pattern calibration manquant détecté par F821) |
| 2026-04-10 | `fetch_nhl_odds` utilisait API-Sports Hockey qui en plan Free retourne `results: 0` systématiquement (pas une erreur, juste aucune data hockey) — `odds_json` toujours vide, les value bets NHL invisibles sur le dashboard, et filtre fenêtre `today 00:00→23:59 UTC` ratait les matchs qui commencent après minuit UTC côté nuit US | Vérifier en live que le provider Free retourne vraiment les données promises (pas juste HTTP 200) AVANT de baser un scheduler dessus ; pour les fenêtres temporelles, préférer `now → now + 36h` aux bornes calendrier UTC qui coupent arbitrairement les soirées transcontinentales |
| 2026-04-10 | Team names divergent entre providers : `'St. Louis Blues'` (NHL API) vs `'St Louis Blues'` (The Odds API) — point. Et `'Utah Hockey Club'` vs `'Utah Mammoth'` — rename officiel 2025-26 qu'un provider reflète et pas l'autre | Quand on matche des entités entre deux APIs externes, normaliser agressivement (lowercase, strip punct, drop les suffixes "Hockey Club"/"Mammoth"/etc.) et maintenir un mapping explicite pour les renames officiels |
| 2026-04-18 | eval_set=(X_test, y_test) dans 2 endroits train.py — leakage confirmé → validation split dédié | Toujours créer X_val depuis X_train avec une tranche temporelle AVANT le training ; eval_set = X_val, test set = intouchable jusqu'à l'évaluation finale |
| 2026-04-19 | /api/admin/update-scores exposé publiquement "No auth required when called internally" — DoS possible sur quota API-Football payant | Tout endpoint /admin/* DOIT avoir Depends(verify_internal_auth) — zéro exception "interne", un attaquant peut toujours forger la requête |
| 2026-04-17 | NHL fallback ML → Poisson était silencieux : shot_predictor.load() échouait sans WARNING, ~40% des requêtes SHOT/ASSIST utilisaient Poisson (~0.24 Brier) à la place de XGBoost (~0.18) sans aucun log | Tout predictor ML doit exposer un attribut .loaded: bool = False, défini à True seulement après load() réussi. Logger WARNING explicitement dans chaque branche fallback. Exposer ml_fallback_used dans la response JSON pour rendre l'état visible côté client et monitoring. |
| 2026-04-21 | `npx trigger.dev@latest deploy` échoue en CI si @trigger.dev/sdk et @trigger.dev/build ne matchent pas exactement la version CLI — erreur "Version mismatch detected" | Avant tout deploy Trigger.dev, aligner @trigger.dev/sdk + @trigger.dev/build sur la version CLI exacte : `npm install @trigger.dev/sdk@X.Y.Z @trigger.dev/build@X.Y.Z` |
| 2026-04-21 | worker.py/APScheduler = mort-code en prod — tous les crons passent par Trigger.dev Cloud + endpoints FastAPI /api/trigger/*. Les commentaires "// ⚠️ DISABLED — consolidated into worker.py" dans les fichiers .ts sont obsolètes et trompeurs | Ne pas se fier aux commentaires DISABLED dans trigger-worker/src/trigger/*.ts : les tasks Trigger.dev tournent en prod indépendamment de worker.py. APScheduler n'est pas lancé sur Railway. |
| 2026-04-21 | The Odds API : btts requiert /events/{id}/odds (pas bulk), Over 1.5 rarement renvoyé par défaut. Betclic et ZEbet absents du smoke test → keys API peut-être erronées ou non couvertes par le plan actuel | Valider les bookmakers couverts par le plan Odds API AVANT de les ajouter au registry. Tester chaque book individuellement avec un appel direct avant intégration. |
| 2026-04-22 | Plan Lot 2 T02/T03 référençait `from api.auth import current_user` mais la fonction n'existait pas — seul `_require_admin` existait pour les endpoints admin | Avant d'exécuter un plan, grep les symboles externalisés (`from x import y`) pour vérifier qu'ils existent, sinon prévoir de les créer dans la même PR avec un pattern existant du codebase comme référence |
| 2026-04-22 | T02/T03 endpoints V2 codés sur un schéma fantôme (`match_date`, `sport`, `selection`, `confidence` sur `predictions`, vue inexistante `matches_v2_view`) — aurait crashé à la première requête prod | Toujours valider les colonnes réelles via API prod (`/api/predictions?date=...`) ou Supabase MCP AVANT d'écrire la query — ne jamais inventer un schéma à partir d'un spec frontend |
| 2026-04-22 | `mock_supabase` fixture conftest ne listait pas `is_` dans les chain methods → `.is_("result","null")` cassait la chaîne et le side_effect pointait sur la mauvaise query, résultats décalés sans erreur visible | Quand on ajoute une méthode Supabase (`is_`, `not_`, `contains`, etc.) dans le code, ajouter immédiatement au tuple de chain methods dans `tests/conftest.py` — sinon les mocks consomment les slots à l'envers silencieusement |
| 2026-04-22 | `useSafePick` typé `SafePick` mais backend retourne `{date, safe_pick, fallback_message}` → `data.odd` undefined → `.toFixed(2)` crash → blank page toutes pages (pas d'ErrorBoundary sur AppV2). MSW handler `/api/matches` retournait la shape frontend plate alors que `useMatchesOfDay` a un adapter attendant la shape backend groupée → 50 tests CI rouges silencieusement | (1) Tout hook qui consomme une API doit typer la réponse backend exacte et utiliser `select:` pour adapter. (2) Les MSW handlers doivent retourner la shape BACKEND réelle, pas la shape frontend — sinon l'adapter n'est jamais testé. (3) Toute app React de prod doit avoir un ErrorBoundary à la racine — un seul composant qui throw ne doit jamais pouvoir blanchir toutes les pages. (4) Tout endpoint frontend-critique doit avoir un contract test qui épingle la shape attendue |
| 2026-04-26 | Le hook du Safe du jour typait la réponse comme la shape frontend alors que le backend retourne `{date, safe_pick, fallback_message}` ; la card appelait `.toFixed()` sur `undefined` et `AppV2` n'avait pas d'ErrorBoundary, provoquant une page blanche globale | Tout hook API doit typer la réponse backend réelle puis adapter explicitement la shape frontend. Toute app React de prod doit avoir un ErrorBoundary à la racine pour qu'un composant qui throw ne puisse pas blanchir toutes les routes |
| 2026-04-26 | Des fichiers `tests/test_api_cs*.py` et `tests/test_cleansheets.py` faisaient des appels API-Football live à l'import, et MSW `/api/matches` renvoyait l'ancienne shape frontend plate alors que le hook V2 attend la shape backend groupée | Aucun test standard ne doit dépendre du réseau à l'import : les probes live doivent être des tests `integration` opt-in. Les mocks MSW doivent refléter le contrat backend réel, pas une shape frontend historique |
| 2026-04-26 | Auto-training ML sans registre versionné écraserait les modèles actifs et rendrait les décisions de promotion impossibles à auditer | Toute boucle d'apprentissage automatique doit commencer par un registre MLOps versionné : runs, versions candidates, snapshots de performance, décisions promote/reject et rollback avant tout retraining automatique |
| 2026-04-26 | Un endpoint MLOps `/evaluate` qui ne persiste rien donnerait une fausse impression de monitoring actif | Tout job de monitoring doit écrire un snapshot daté en base avec fenêtre, modèle, sample_size et métriques calculées ; un stub n'est acceptable que derrière un test rouge qui force la persistance réelle ensuite |
| 2026-04-27 | Un endpoint MLOps `/auto-train` qui retourne seulement une policy laisse croire que l'apprentissage est branché alors qu'aucun candidat ni décision auditée n'existent | Un job d'auto-training doit relier explicitement policy -> training_run -> candidate_version -> décision promote/reject/manual_review ; sinon retourner `skipped` ou `training_failed` plutôt qu'un succès ambigu |
| 2026-04-27 | Un redesign visuel de dashboard de value betting peut devenir décoratif s'il commence par les couleurs au lieu de la décision utilisateur | Pour l'UI premium ProbaLab, cadrer chaque écran autour d'un cockpit de décision : signal value, risque bankroll, preuve de calibration et dernière mise à jour avant les effets visuels |
| 2026-04-27 | La page détail match exposait reco, probas, cotes et bankroll dans des cartes séparées, ce qui forçait l'utilisateur à recomposer la décision lui-même | Sur une page match, regrouper le ticket conseillé, fair odds, edge, Kelly, risque bankroll et fraîcheur des cotes dans un bloc décisionnel unique avant les détails secondaires |
| 2026-04-27 | Le wording premium trop orienté trading/value donnait une identité de tipster financier alors que le positionnement validé est analyses et probabilités sportives | Le premier niveau UX doit vendre la compréhension du match : probabilités, confiance modèle, scénario probable et explication IA ; les notions value/Kelly/bankroll restent en détails avancés |
| 2026-04-27 | Des termes comme CLV, Pinnacle, quant fund, Kelly ou Brier donnent une impression experte mais excluent même des parieurs aguerris | En surface UX, remplacer le jargon par des labels compréhensibles : signal marché, qualité des probabilités, mise prudente, prono recommandé ; garder les termes techniques seulement en aide contextuelle avancée |
| 2026-04-27 | Les listes de matchs affichaient encore `SAFE` et des pourcentages de signal isolés, ce qui ramenait l'UX vers un code interne plutôt qu'une aide à la décision | Dans les listes visibles, les chips doivent expliquer le rôle du match : `Prono recommandé`, `Signal +x%`, `Confiance élevée`, etc. ; éviter les acronymes et badges internes sans contexte |
| 2026-04-27 | Les métriques premium (`ROI`, `Accuracy`, `Win rate`, `Drawdown`) faisaient sérieux mais donnaient un ton finance/statistiques trop froid pour le positionnement parieur | Renommer les preuves de performance en langage utilisateur : `Résultat`, `Performance`, `Précision`, `Taux de réussite`, `Baisse max`, `Capital suivi` ; garder les noms techniques dans le code uniquement |
| 2026-04-27 | L'espace compte affichait encore `bankroll` dans la navigation, les modales et les CTA, ce qui peut sembler technique ou anglicisé | En UX française, privilégier `capital` ou `suivi du capital` ; conserver `bankroll` seulement dans les routes, schémas et noms internes déjà câblés |
| 2026-04-27 | Le header V2 était trop plat : simple texte logo + liens, pas assez premium pour porter l'identité `analyses et probabilités sportives` | Le header principal doit porter l'identité produit dès le premier pixel : marque visuelle, sous-titre métier, navigation en pills, état actif fort et CTA premium discret mais visible |
| 2026-04-27 | Le layout V2 rendait le header avec le rôle par défaut `visitor`, donc les admins gardaient les routes `/admin` et `/performance` mais perdaient l'accès visible dans la navigation | Le shell applicatif doit recevoir le rôle réel via `useV2User()` et exposer les entrées admin uniquement pour `role === 'admin'` ; ne jamais cacher une surface de pilotage existante lors d'une refonte navigation |
| 2026-04-27 | La page match empilait hero, probas, reco, stats et marchés sans raconter la décision, ce qui faisait moins premium et demandait trop d'effort à l'utilisateur | Une fiche match doit commencer par une synthèse décisionnelle : équipes, horaire, probabilités clés, prono éventuel, confiance et scénario à surveiller ; les stats, historique et marchés viennent ensuite en preuves |
| 2026-04-27 | La page liste des matchs mettait les filtres et les lignes compactes au premier plan, ce qui donnait une impression back-office plutôt qu'une sélection premium | La page `/matchs` doit commencer par une synthèse du jour puis présenter chaque match comme une carte de décision avec probabilités, lecture rapide, signaux et CTA d'analyse ; les filtres restent secondaires |
| 2026-04-27 | Les E2E Playwright sur `vite preview` voyaient `import.meta.env.DEV === false` : MSW ne démarrait pas, `__e2e_role__` était ignoré dans `useV2User`, et les `data-testid` avaient divergé du redesign | Build CI E2E dédié avec `VITE_E2E` + `VITE_MSW_PREVIEW` (+ `VITE_MSW_ENABLED` / API localhost) ; activer MSW et la lecture du rôle E2E hors dev uniquement dans ce mode ; garder `data-testid` stables ou aligner les specs |
| 2026-04-27 | E2E : clic sur `match-row` sans cibler le `Link` ne naviguait pas ; handler MSW `POST` règles notif. renvoyait 201 mais `GET` restait sur les fixtures — la règle créée n'apparaissait jamais | Dans les specs, suivre le lien (ex. rôle + nom « Ouvrir l'analyse ») + `waitForURL` ; pour MSW, stock mutable partagé entre GET/POST/PUT/PATCH/DELETE + reset dans `afterEach` avec `resetHandlers` |
| 2026-04-30 | La liste V2 affichait des matchs NHL a 0% car `/api/matches` renvoyait `prediction: null` et le frontend convertissait l'absence en zero | Ne jamais convertir une prediction absente en 0 : propager `null`, afficher un etat disponible bientot, et mapper les probas/scores NHL dans le contrat `/api/matches` |
| 2026-04-30 | Le build V2 chargeait toutes les pages au démarrage (routes V2 + pages legacy admin/login/perf), produisant un chunk principal de 1.38 MB et une app perçue lente | Les routes applicatives doivent être lazy-loadées derrière `Suspense`; les E2E MSW ne doivent pas promettre une persistance après hard reload si l'état mocké vit côté worker preview |
