import { defineConfig } from "@trigger.dev/sdk/v3";

export default defineConfig({
    project: "proj_mynrjjbzdvaqytzpyfjg",
    dirs: ["./src/trigger"],
    maxDuration: 3600, // 1 hour max duration for tasks
});
